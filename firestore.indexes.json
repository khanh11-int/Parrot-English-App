{
  "indexes": [
    {
      "_comment": [
        "XP nhóm tính bằng aggregate sum('experience') trên users where",
        "groupId == <id>. Firestore đòi composite index cho sum/average khi có",
        "filter — count() thì không cần. Khai báo ở đây thay vì bấm link trong",
        "log lỗi, để ai clone repo về cũng chỉ cần:",
        "  firebase deploy --only firestore:indexes"
      ],
      "collectionGroup": "users",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "groupId", "order": "ASCENDING" },
        { "fieldPath": "experience", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
